apiVersion: v1
kind: Namespace
metadata:
  name: podland
  labels:
    name: podland
---
# Note: PostgreSQL secrets are now managed via SealedSecret
# See: secrets/postgres-sealedsecret.yaml
# Apply with: kubeseal --format yaml < secret.yaml > secrets/postgres-sealedsecret.yaml
